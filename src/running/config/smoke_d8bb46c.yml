# =============================================================================
# Smoke test — OCaml commit d8bb46c (== 5.5.0-beta1), all macrobenchmarks
# =============================================================================
# One runtime, full benchmark set, one invocation.  Exercises the compiler
# build and every benchmark's build+run path before committing to the full
# 8-variant sweep.
#
# Usage:
#   RUNNING_MACRO_BENCH_DIR=~/macro-benches \
#   CONFIG_FILE=src/running/config/smoke_d8bb46c.yml \
#   LOG_DIR=/tmp/smoke_d8bb46c_logs \
#     bash run_ocaml_bench_gc_sweep.sh
# =============================================================================

includes:
  - "./macrobenchmarks_base.yml"

overrides:
  invocations: 1

runtimes:
  ocaml-d8bb46c:
    type: OCaml
    commit: "d8bb46c39bf5fcafb513a8ba18e667d3f8c2600a"

configs:
  - "ocaml-d8bb46c|perf_grp1|re-25|md-2"
