includes:
  - "./jvms.yml"
  - "./suites.yml"
  - "./modifiers.yml"

# Can override with command line argument -i
invocations: 10
# Can override with command line argument -m
minheap_multiplier: 1.0
# Compress each benchmark log to .log.gz after the run.
compress_logs: true
heap_range: 6
spread_factor: 1
remote_host: "squirrel.moma"
